0) Fix the cardItem component. Weird things happen when the slot content is too big and container size is too small
  a) Consider using em's or making the font small. In particular, the top slot should be 1.5 font high

