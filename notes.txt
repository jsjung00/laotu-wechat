Errors:
  cloud init error:  Error: getaddrinfo ENOTFOUND servicewechat.com
  


For the store, consider having a image upload functionality so the admin can upload an image and convert to URL in the database of products

Consider changing the ordering of the events. Right now it is in chronological order, but that may mean that too many events are far
ahead in advance.


Consider changing the datastructure such that the key of the tab is not an number but rather the title

I want the swiper to at the min cover the entire screen, be large enough to contain the table of events

Consider making Profile Text size dynamic based on width of container w/o JS (too slow)

I want to build a tabbar
  a) I could build a tabs using WEUI, want to figure out how to get the text in the middle
  b) I could build a custom tabbar just following the tutorials

BUG: navbar is overlapping with the tab bar. 
  a) build a component that contains both the navbar and the tabbar
  b) add extra space to the tabbar on top by calculating the height of the tabbar
  c) try to figure out how to get the tabbar below the navbar




  


